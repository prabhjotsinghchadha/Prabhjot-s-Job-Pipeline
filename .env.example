# Copy to .env and fill in ONE of the following.
# .env is gitignored; never commit real values.

# Option 1 — Claude Pro/Max subscription (no extra cost).
# Generate with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Option 2 — Anthropic API credits (pay per token).
# Create at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
